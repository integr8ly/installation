{
    "components":[
        {% for c in components %}
            {
                "name": "{{c.name}}",
                "version": "{{c.version}}",
                "host": "{{c.host}}"
            }
            {% if not loop.last %}
            ,
            {% endif %}
        {% endfor %}
    ]
}