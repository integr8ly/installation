backed_up_product_namespaces:
  - "{{threescale_namespace}}"
  - "{{rhsso_namespace}}"
  - "{{che_namespace}}"
  - "{{enmasse_namespace}}"
  - "{{fuse_namespace}}"

backup_credentials_secret: 'aws-credentials'
backup_credentials_ns: 'default'
backup_resources_ns:
  - "{{backup_resources_location}}/backup-cronjob-template.yaml"
  - "{{backup_resources_location}}/rbac/service-account.yaml"
  - "{{backup_resources_location}}/rbac/role-binding.yaml"
backup_resources_cluster:
  - "{{backup_resources_location}}/rbac/role.yaml"