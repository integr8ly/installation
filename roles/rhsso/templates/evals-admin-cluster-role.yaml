apiVersion: authorization.openshift.io/v1
kind: ClusterRole
metadata:
  name: integreatly-evals-admin
rules:
  - apiGroups: [""]
    resources: ["secrets"]
    verbs: ["get"]
    resourceNames: ["inventory"]