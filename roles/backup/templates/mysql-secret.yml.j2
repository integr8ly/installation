apiVersion: v1
kind: Secret
metadata:
  name: {{ backup_secret_name }}
type: Opaque
stringData:
  MYSQL_HOST: "{{ host }}"
  MYSQL_PORT: "{{ port }}"
  MYSQL_USER: "{{ user }}"
  MYSQL_PASSWORD: "{{ password }}"
